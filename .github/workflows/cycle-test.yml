name: Cycle Test Workflow

on:
  workflow_dispatch:
    inputs:
      test_message:
        description: 'Test message for cycle validation'
        required: false
        default: 'Cycle validation test'

jobs:
  cycle-test:
    runs-on: self-hosted
    timeout-minutes: 2
    steps:
      - name: Cycle Test Step
        run: |
          echo "🔄 CYCLE VALIDATION TEST"
          echo "Message: ${{ github.event.inputs.test_message }}"
          echo "Timestamp: $(date)"
          echo "✅ Cycle test completed"