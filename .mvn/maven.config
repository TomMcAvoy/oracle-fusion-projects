# 🔐 Maven Configuration with Vault-backed Properties
# Environment variables will be available during Maven execution
-Dgithub.token=${GITHUB_TOKEN}
-Dgitlab.token=${GITLAB_TOKEN}
-Dvault.enabled=true
